function [new_array] = compare_val(user_v, v,array,real_val_array)
reply = input('Is the shock more or less previous shock? Press 1  or 0 for less:');
comp = (user_val>= v)

new_array = [array,comp]


end
